diff --git a/backends/confidential-guest-support.c b/backends/confidential-guest-support.c
index 156dd15e6677e27fc2693d55ee85ed5fb17be2a9..337b3bd9a7fe3e2e31b796aec635ac6b5cbd34ea 100644
--- a/backends/confidential-guest-support.c
+++ b/backends/confidential-guest-support.c
@@ -15,12 +15,20 @@
 
 #include "system/confidential-guest-support.h"
 #include "qapi/error.h"
+#include "system/address-spaces.h"
 
 OBJECT_DEFINE_ABSTRACT_TYPE(ConfidentialGuestSupport,
                             confidential_guest_support,
                             CONFIDENTIAL_GUEST_SUPPORT,
                             OBJECT)
 
+static AddressSpace *get_dma_as(ConfidentialGuestSupport *cgs)
+{
+    g_assert(cgs);
+
+    return &address_space_memory;
+}
+
 static bool check_support(ConfidentialGuestPlatformType platform,
                          uint16_t platform_version, uint8_t highest_vtl,
                          uint64_t shared_gpa_boundary)
@@ -62,6 +70,7 @@ static void confidential_guest_support_class_init(ObjectClass *oc,
                                                   const void *data)
 {
     ConfidentialGuestSupportClass *cgsc = CONFIDENTIAL_GUEST_SUPPORT_CLASS(oc);
+    cgsc->get_dma_as = get_dma_as;
     cgsc->check_support = check_support;
     cgsc->set_guest_state = set_guest_state;
     cgsc->set_guest_policy = set_guest_policy;
diff --git a/hw/virtio/virtio-mmio.c b/hw/virtio/virtio-mmio.c
index 55ceaeef5fc7e2d06a034393999b942d6b610a4f..0414430b99b4968914fec76f355c0a73cb85fcb4 100644
--- a/hw/virtio/virtio-mmio.c
+++ b/hw/virtio/virtio-mmio.c
@@ -21,6 +21,7 @@
 
 #include "qemu/osdep.h"
 #include "standard-headers/linux/virtio_mmio.h"
+#include "hw/core/boards.h"
 #include "hw/core/irq.h"
 #include "hw/core/qdev-properties.h"
 #include "hw/core/sysbus.h"
@@ -33,9 +34,25 @@
 #include "hw/virtio/virtio-mmio.h"
 #include "qemu/error-report.h"
 #include "qemu/log.h"
+#include "system/address-spaces.h"
+#include "system/confidential-guest-support.h"
 #include "trace.h"
 #include "qapi/error.h"
 
+static AddressSpace *virtio_mmio_get_dma_as(DeviceState *d)
+{
+    MachineState *machine = MACHINE(qdev_get_machine());
+    ConfidentialGuestSupport *cgs = machine->cgs;
+    ConfidentialGuestSupportClass *cgsc;
+
+    if (!cgs) {
+        return &address_space_memory;
+    }
+
+    cgsc = CONFIDENTIAL_GUEST_SUPPORT_GET_CLASS(cgs);
+    return cgsc->get_dma_as(cgs);
+}
+
 static bool virtio_mmio_ioeventfd_enabled(DeviceState *d)
 {
     VirtIOMMIOProxy *proxy = VIRTIO_MMIO(d);
@@ -838,6 +855,7 @@ static void virtio_mmio_bus_class_init(ObjectClass *klass, const void *data)
     k->ioeventfd_assign = virtio_mmio_ioeventfd_assign;
     k->pre_plugged = virtio_mmio_pre_plugged;
     k->vmstate_change = virtio_mmio_vmstate_change;
+    k->get_dma_as_without_iommu = virtio_mmio_get_dma_as;
     k->has_variable_vring_alignment = true;
     bus_class->max_dev = 1;
     bus_class->get_dev_path = virtio_mmio_bus_get_dev_path;
diff --git a/hw/virtio/virtio-bus.c b/hw/virtio/virtio-bus.c
--- a/hw/virtio/virtio-bus.c
+++ b/hw/virtio/virtio-bus.c
@@ -83,5 +83,8 @@ void virtio_bus_device_plugged(VirtIODevice *vdev, Error **errp)
 
     vdev->dma_as = &address_space_memory;
+    if (klass->get_dma_as_without_iommu) {
+        vdev->dma_as = klass->get_dma_as_without_iommu(qbus->parent);
+    }
     if (has_iommu) {
         vdev_has_iommu = virtio_host_has_feature(vdev, VIRTIO_F_IOMMU_PLATFORM);
         /*
diff --git a/include/hw/virtio/virtio-bus.h b/include/hw/virtio/virtio-bus.h
--- a/include/hw/virtio/virtio-bus.h
+++ b/include/hw/virtio/virtio-bus.h
@@ -93,6 +93,7 @@ typedef struct VirtioBusClass {
      * Note that changing this will break migration for this transport.
      */
     bool has_variable_vring_alignment;
+    AddressSpace *(*get_dma_as_without_iommu)(DeviceState *d);
     AddressSpace *(*get_dma_as)(DeviceState *d);
     bool (*iommu_enabled)(DeviceState *d);
 };
diff --git a/include/system/confidential-guest-support.h b/include/system/confidential-guest-support.h
index 5dca7173088032f463f2e95767fe5bcaf1d0ee53..90c0dded7516d4f886e3b00dadb0dbba9f63de3b 100644
--- a/include/system/confidential-guest-support.h
+++ b/include/system/confidential-guest-support.h
@@ -100,6 +100,12 @@ typedef struct ConfidentialGuestSupportClass {
     int (*kvm_init)(ConfidentialGuestSupport *cgs, Error **errp);
     int (*kvm_reset)(ConfidentialGuestSupport *cgs, Error **errp);
 
+    /*
+     * Return the address space through which emulated devices access
+     * guest DMA buffers.
+     */
+    AddressSpace *(*get_dma_as)(ConfidentialGuestSupport *cgs);
+
     /*
      * Check to see if this confidential guest supports a particular
      * platform or configuration.
diff --git a/target/arm/kvm-rme.c b/target/arm/kvm-rme.c
index 7b83de2f956b3e672ab4a798b05e1259274d8c53..f6409c21685c575ebf366a82218828ba44f6ffe9 100644
--- a/target/arm/kvm-rme.c
+++ b/target/arm/kvm-rme.c
@@ -18,6 +18,7 @@
 #include "qemu/error-report.h"
 #include "qemu/units.h"
 #include "qom/object_interfaces.h"
+#include "system/address-spaces.h"
 #include "system/confidential-guest-support.h"
 #include "system/kvm.h"
 #include "system/runstate.h"
@@ -209,11 +210,19 @@ void kvm_arm_rme_vcpu_init(ARMCPU *cpu)
     cpu->kvm_rme = true;
 }
 
+static AddressSpace *rme_get_dma_as(ConfidentialGuestSupport *cgs)
+{
+    RmeGuest *guest = RME_GUEST(cgs);
+
+    return guest->dma_region ? &guest->dma_as : &address_space_memory;
+}
+
 static void rme_guest_class_init(ObjectClass *oc, const void *data)
 {
     ConfidentialGuestSupportClass *klass = CONFIDENTIAL_GUEST_SUPPORT_CLASS(oc);
 
     klass->kvm_init = kvm_arm_rme_init;
+    klass->get_dma_as = rme_get_dma_as;
 }
 
 static void rme_guest_init(Object *obj)
