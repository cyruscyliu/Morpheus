{
  "name": "@morpheus/microkit-sdk",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "bin": {
    "microkit-sdk": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm exec tsc -p tsconfig.json && node --test test/*.test.js",
    "smoke": "pnpm exec tsc -p tsconfig.json && node scripts/smoke.mjs"
  }
}
