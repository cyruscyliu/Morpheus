diff --git a/libafl/tcg.c b/libafl/tcg.c
index c155621..0000000 100644
--- a/libafl/tcg.c
+++ b/libafl/tcg.c
@@ -103,9 +103,13 @@ TranslationBlock* libafl_gen_edge(CPUState* cpu, vaddr src_block,
     assert_memory_lock();
     qemu_thread_jit_write();

-    // TODO: this (get_page_addr_code_hostp) is a bottleneck in systemmode,
-    // investigate why
-    phys_pc = get_page_addr_code_hostp(env, src_block, &host_pc);
+#ifdef CONFIG_USER_ONLY
+    phys_pc = get_page_addr_code_hostp(env, s.pc, &host_pc);
     phys_pc ^= reverse_bits((tb_page_addr_t)exit_n);
+#else
+    /* Synthetic edge TBs are temporary and are not backed by guest RAM. */
+    phys_pc = -1;
+    host_pc = NULL;
+#endif

     // if (phys_pc == -1) {
