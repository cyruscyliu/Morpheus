## 1. Define the new boundary

- [x] 1.1 Define the Morpheus remote command tree for managed single-tool runs
- [x] 1.2 Define the initial Buildroot-backed Morpheus remote run inputs and
      JSON outputs
- [x] 1.3 Document that remote workspaces are supported only through Morpheus

## 2. Move remote Buildroot orchestration

- [x] 2.1 Implement Buildroot-backed remote managed runs in Morpheus
- [x] 2.2 Implement Morpheus remote inspect, logs, and fetch behavior
- [x] 2.3 Remove `remote-*` command handling from the Buildroot CLI

## 3. Reconcile documentation and validation

- [x] 3.1 Update Buildroot help text, README examples, and tests
- [x] 3.2 Add Morpheus docs and tests for remote Buildroot runs
- [x] 3.3 Verify migration examples and JSON contracts end to end
