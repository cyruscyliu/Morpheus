{
  "name": "@morpheus/cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "morpheus": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --loader ts-node/esm src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/index.test.js"
  },
  "dependencies": {
    "@morpheus/core": "workspace:*",
    "@morpheus/tool-registry": "workspace:*",
    "@morpheus/workflows": "workspace:*"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
