diff --git a/drivers/virtio/virtio_ring.c b/drivers/virtio/virtio_ring.c
@@ -11,8 +11,10 @@
 #include <linux/module.h>
 #include <linux/hrtimer.h>
 #include <linux/dma-mapping.h>
+#include <linux/dma_telemetry_aperture.h>
 #include <linux/kmsan.h>
 #include <linux/spinlock.h>
+#include <linux/string.h>
 #include <xen/xen.h>
 
 #ifdef DEBUG
@@ -224,6 +226,11 @@
 
 static struct vring_desc_extra *vring_alloc_desc_extra(unsigned int num);
 static void vring_free(struct virtqueue *_vq);
+static int vring_mapping_error(const struct vring_virtqueue *vq,
+			       dma_addr_t addr,
+			       enum dma_data_direction direction,
+			       size_t size,
+			       const char *kind);
 
 /*
  * Helpers.
@@ -293,6 +300,138 @@
 	return vring->use_map_api && (extra->addr != DMA_MAPPING_ERROR);
 }
 
+/*
+ * Log DMA visibility and a small set of virtqueue progress events from
+ * virtio_ring.c, where both the sharing decisions and used-ring harvesting
+ * are centralized.
+ */
+static atomic_t hp_dma_trace_seq = ATOMIC_INIT(0);
+
+static bool hp_dma_trace_via_mmio(struct virtio_device *vdev)
+{
+	if (!vdev->config || !vdev->config->set)
+		return false;
+	return true;
+}
+
+static void hp_dma_trace_publish(struct virtio_device *vdev, u32 opcode,
+				 enum dma_data_direction dir, size_t size,
+				 dma_addr_t addr)
+{
+	__le32 event;
+	__le32 addr_lo;
+	__le32 addr_hi;
+	__le32 len;
+	u32 sequence;
+	u32 size32 = size > U32_MAX ? U32_MAX : (u32)size;
+
+	if (!hp_dma_trace_via_mmio(vdev))
+		return;
+
+	/*
+	 * Publish the payload first and commit with HP_DMA_EVENT last so EL2
+	 * observers never decode a partially updated record.
+	 */
+	sequence = (u32)atomic_inc_return(&hp_dma_trace_seq) &
+		   (HP_DMA_EVENT_SEQUENCE_MASK >>
+		    HP_DMA_EVENT_SEQUENCE_SHIFT);
+	addr_lo = cpu_to_le32(hp_dma_addr_lo((u64)addr));
+	addr_hi = cpu_to_le32(hp_dma_addr_hi((u64)addr));
+	len = cpu_to_le32(size32);
+	event = cpu_to_le32(HP_DMA_EVENT_ENCODE(opcode,
+						hp_dma_event_dir(dir),
+						sequence));
+
+	vdev->config->set(vdev, HP_DMA_ADDR_LO_OFFSET, &addr_lo,
+			  sizeof(addr_lo));
+	vdev->config->set(vdev, HP_DMA_ADDR_HI_OFFSET, &addr_hi,
+			  sizeof(addr_hi));
+	vdev->config->set(vdev, HP_DMA_LEN_OFFSET, &len,
+			  sizeof(len));
+	vdev->config->set(vdev, HP_DMA_EVENT_OFFSET, &event,
+			  sizeof(event));
+}
+
+#define HP_DMA_OP_NAME(_op)						\
+	((_op) == HP_DMA_EVENT_OP_ALLOC_SUCCESS ? "alloc" :		\
+	 (_op) == HP_DMA_EVENT_OP_ALLOC_FAIL ? "alloc_fail" :		\
+	 (_op) == HP_DMA_EVENT_OP_FREE ? "free" :			\
+	 (_op) == HP_DMA_EVENT_OP_MAP ? "map" :				\
+	 (_op) == HP_DMA_EVENT_OP_MAP_FAIL ? "map_fail" :		\
+	 (_op) == HP_DMA_EVENT_OP_UNMAP ? "unmap" :			\
+	 (_op) == HP_DMA_EVENT_OP_SYNC_FOR_CPU ? "sync_for_cpu" :	\
+	 (_op) == HP_DMA_EVENT_OP_SYNC_FOR_DEVICE ? "sync_for_device" : \
+	 (_op) == HP_DMA_EVENT_OP_VQ_POLL_HIT ? "vq_poll_hit" :	\
+	 (_op) == HP_DMA_EVENT_OP_VQ_POLL_MISS ? "vq_poll_miss" :	\
+	 (_op) == HP_DMA_EVENT_OP_VQ_GET_BUF ? "vq_get_buf" :		\
+	 (_op) == HP_DMA_EVENT_OP_VQ_GET_BUF_EMPTY ?			\
+	 "vq_get_buf_empty" : "unknown")
+
+#define HP_DMA_DIR_NAME(_dir)						\
+	((_dir) == HP_DMA_EVENT_DIR_BIDIRECTIONAL ? "bidirectional" :	\
+	 (_dir) == HP_DMA_EVENT_DIR_TO_DEVICE ? "to_device" :		\
+	 (_dir) == HP_DMA_EVENT_DIR_FROM_DEVICE ? "from_device" :	\
+	 (_dir) == HP_DMA_EVENT_DIR_NONE ? "none" : "unknown")
+
+#define HP_DMA_TRACE(_vdev, _op, _dir, _size, _addr, _kind, _use_dma)	\
+	do {								\
+		u32 hp_dma_trace_dir = hp_dma_event_dir(_dir);		\
+		dma_addr_t hp_dma_trace_addr = (_addr);			\
+		/* The trapped config-space write is the telemetry signal.
+		 * EL2 can observe it even if later software ignores the
+		 * payload contents.
+		 */							\
+		hp_dma_trace_publish((_vdev), (_op), (_dir), (_size),	\
+				     hp_dma_trace_addr);			\
+									\
+		dev_info_ratelimited(&(_vdev)->dev,			\
+				     "virtio_telemetry op=%s kind=%s "	\
+				     "dir=%s size=%zu aux=%pad "	\
+				     "path=%s\n",			\
+				     HP_DMA_OP_NAME(_op), (_kind),	\
+				     HP_DMA_DIR_NAME(hp_dma_trace_dir),	\
+				     (size_t)(_size),			\
+				     &hp_dma_trace_addr,		\
+				     (_use_dma) ? "dma_api" : "phys");	\
+	} while (0)
+
+static void hp_dma_trace_preview(char *out, size_t out_size,
+				 const void *buf, size_t size)
+{
+	static const char hex[] = "0123456789abcdef";
+	const u8 *bytes = buf;
+	size_t preview = min_t(size_t, size, 16);
+	size_t i;
+
+	if (!out_size)
+		return;
+	if (!buf || !preview) {
+		out[0] = '\0';
+		return;
+	}
+	for (i = 0; i < preview && (i * 2 + 1) < out_size; ++i) {
+		out[i * 2] = hex[(bytes[i] >> 4) & 0xf];
+		out[i * 2 + 1] = hex[bytes[i] & 0xf];
+	}
+	out[min_t(size_t, preview * 2, out_size - 1)] = '\0';
+}
+
+static bool hp_dma_trace_submit_on_map(enum dma_data_direction dir)
+{
+	return dir == DMA_TO_DEVICE || dir == DMA_BIDIRECTIONAL;
+}
+
+static bool hp_dma_trace_complete_on_cpu_sync(enum dma_data_direction dir)
+{
+	return dir == DMA_FROM_DEVICE || dir == DMA_BIDIRECTIONAL;
+}
+
+static void hp_dma_trace_vq_state(struct virtqueue *_vq, u32 opcode,
+				  dma_addr_t aux, const char *kind)
+{
+	HP_DMA_TRACE(_vq->vdev, opcode, DMA_NONE, 0, aux, kind, false);
+}
+
 size_t virtio_max_dma_size(const struct virtio_device *vdev)
 {
 	size_t max_segment_size = SIZE_MAX;
@@ -315,8 +422,18 @@
 			       union virtio_map map)
 {
 	if (vring_use_map_api(vdev)) {
-		return virtqueue_map_alloc_coherent(vdev, map, size,
-						    map_handle, flag);
+		void *queue = virtqueue_map_alloc_coherent(vdev, map, size,
+							   map_handle, flag);
+
+		if (queue)
+			HP_DMA_TRACE(vdev, HP_DMA_EVENT_OP_ALLOC_SUCCESS,
+				     DMA_BIDIRECTIONAL, size, *map_handle,
+				     "queue", true);
+		else
+			HP_DMA_TRACE(vdev, HP_DMA_EVENT_OP_ALLOC_FAIL,
+				     DMA_BIDIRECTIONAL, size, 0,
+				     "queue", true);
+		return queue;
 	} else {
 		void *queue = alloc_pages_exact(PAGE_ALIGN(size), flag);
 
@@ -337,8 +454,27 @@
 			 */
 			if (WARN_ON_ONCE(*map_handle != phys_addr)) {
 				free_pages_exact(queue, PAGE_ALIGN(size));
+				HP_DMA_TRACE(vdev, HP_DMA_EVENT_OP_ALLOC_FAIL,
+					     DMA_BIDIRECTIONAL, size, 0,
+					     "queue", false);
 				return NULL;
 			}
+			HP_DMA_TRACE(vdev, HP_DMA_EVENT_OP_ALLOC_SUCCESS,
+				     DMA_BIDIRECTIONAL, size, *map_handle,
+				     "queue", false);
+			{
+				char preview[33];
+
+				hp_dma_trace_preview(preview, sizeof(preview),
+						     queue, size);
+				dev_info_ratelimited(&vdev->dev,
+						     "virtio_telemetry_preview op=alloc kind=queue size=%zu aux=%pad preview=%s\n",
+						     size, map_handle, preview);
+			}
+		} else {
+			HP_DMA_TRACE(vdev, HP_DMA_EVENT_OP_ALLOC_FAIL,
+				     DMA_BIDIRECTIONAL, size, 0,
+				     "queue", false);
 		}
 		return queue;
 	}
@@ -348,6 +475,8 @@
 			     void *queue, dma_addr_t map_handle,
 			     union virtio_map map)
 {
+	HP_DMA_TRACE(vdev, HP_DMA_EVENT_OP_FREE, DMA_BIDIRECTIONAL, size,
+		     map_handle, "queue", vring_use_map_api(vdev));
 	if (vring_use_map_api(vdev))
 		virtqueue_map_free_coherent(vdev, map, size,
 					    queue, map_handle);
@@ -366,17 +495,29 @@
 }
 
 static int vring_mapping_error(const struct vring_virtqueue *vq,
-			       dma_addr_t addr)
+			       dma_addr_t addr,
+			       enum dma_data_direction direction,
+			       size_t size,
+			       const char *kind)
 {
 	struct virtio_device *vdev = vq->vq.vdev;
+	int err;
 
 	if (!vq->use_map_api)
 		return 0;
 
 	if (vdev->map)
-		return vdev->map->mapping_error(vq->map, addr);
+		err = vdev->map->mapping_error(vq->map, addr);
 	else
-		return dma_mapping_error(vring_dma_dev(vq), addr);
+		err = dma_mapping_error(vring_dma_dev(vq), addr);
+
+	if (err) {
+		HP_DMA_TRACE(vq->vq.vdev, HP_DMA_EVENT_OP_MAP_FAIL,
+			     direction, size, addr, kind, true);
+		return -ENOMEM;
+	}
+
+	return 0;
 }
 
 /* Map one sg entry. */
@@ -412,9 +553,13 @@
 					 sg->offset, sg->length,
 					 direction, 0);
 
-	if (vring_mapping_error(vq, *addr))
+	if (vring_mapping_error(vq, *addr, direction, *len, "payload"))
 		return -ENOMEM;
 
+	if (hp_dma_trace_submit_on_map(direction))
+		HP_DMA_TRACE(vq->vq.vdev, HP_DMA_EVENT_OP_MAP, direction,
+			     *len, *addr, "payload", true);
+
 	return 0;
 }
 
@@ -425,8 +569,22 @@
 	if (!vq->use_map_api)
 		return (dma_addr_t)virt_to_phys(cpu_addr);
 
-	return virtqueue_map_single_attrs(&vq->vq, cpu_addr,
-					  size, direction, 0);
+	{
+		dma_addr_t addr = virtqueue_map_single_attrs(&vq->vq, cpu_addr,
+							     size, direction, 0);
+
+		if (vq->use_map_api &&
+		    !(vq->vq.vdev->map ?
+		      vq->vq.vdev->map->mapping_error(vq->map, addr) :
+		      dma_mapping_error(vring_dma_dev(vq), addr))) {
+			if (hp_dma_trace_submit_on_map(direction))
+				HP_DMA_TRACE(vq->vq.vdev,
+					     HP_DMA_EVENT_OP_MAP,
+					     direction, size, addr,
+					     "single", true);
+		}
+		return addr;
+	}
 }
 
 static void virtqueue_init(struct vring_virtqueue *vq, u32 num)
@@ -646,7 +807,9 @@
 		dma_addr_t addr = vring_map_single(
 			vq, desc, total_sg * sizeof(struct vring_desc),
 			DMA_TO_DEVICE);
-		if (vring_mapping_error(vq, addr))
+		if (vring_mapping_error(vq, addr, DMA_TO_DEVICE,
+					total_sg * sizeof(struct vring_desc),
+					"indirect"))
 			goto unmap_release;
 
 		virtqueue_add_desc_split(_vq, vq->split.vring.desc,
@@ -830,6 +993,8 @@
 
 	if (!more_used_split(vq)) {
 		pr_debug("No more buffers in queue\n");
+		hp_dma_trace_vq_state(_vq, HP_DMA_EVENT_OP_VQ_GET_BUF_EMPTY,
+				      vq->last_used_idx, "split");
 		END_USE(vq);
 		return NULL;
 	}
@@ -865,6 +1030,8 @@
 				cpu_to_virtio16(_vq->vdev, vq->last_used_idx));
 
 	LAST_ADD_TIME_INVALID(vq);
+	hp_dma_trace_vq_state(_vq, HP_DMA_EVENT_OP_VQ_GET_BUF,
+			      vq->last_used_idx, "split");
 
 	END_USE(vq);
 	return ret;
@@ -922,9 +1089,15 @@
 static bool virtqueue_poll_split(struct virtqueue *_vq, unsigned int last_used_idx)
 {
 	struct vring_virtqueue *vq = to_vvq(_vq);
+	bool pending;
 
-	return (u16)last_used_idx != virtio16_to_cpu(_vq->vdev,
+	pending = (u16)last_used_idx != virtio16_to_cpu(_vq->vdev,
 			vq->split.vring.used->idx);
+	hp_dma_trace_vq_state(_vq,
+			      pending ? HP_DMA_EVENT_OP_VQ_POLL_HIT :
+					HP_DMA_EVENT_OP_VQ_POLL_MISS,
+			      last_used_idx, "split");
+	return pending;
 }
 
 static bool virtqueue_enable_cb_delayed_split(struct virtqueue *_vq)
@@ -1379,7 +1558,9 @@
 	addr = vring_map_single(vq, desc,
 			total_sg * sizeof(struct vring_packed_desc),
 			DMA_TO_DEVICE);
-	if (vring_mapping_error(vq, addr))
+	if (vring_mapping_error(vq, addr, DMA_TO_DEVICE,
+				total_sg * sizeof(struct vring_packed_desc),
+				"indirect"))
 		goto unmap_release;
 
 	vq->packed.vring.desc[head].addr = cpu_to_le64(addr);
@@ -1741,6 +1922,8 @@
 
 	if (!more_used_packed(vq)) {
 		pr_debug("No more buffers in queue\n");
+		hp_dma_trace_vq_state(_vq, HP_DMA_EVENT_OP_VQ_GET_BUF_EMPTY,
+				      READ_ONCE(vq->last_used_idx), "packed");
 		END_USE(vq);
 		return NULL;
 	}
@@ -1787,6 +1970,8 @@
 				cpu_to_le16(vq->last_used_idx));
 
 	LAST_ADD_TIME_INVALID(vq);
+	hp_dma_trace_vq_state(_vq, HP_DMA_EVENT_OP_VQ_GET_BUF,
+			      READ_ONCE(vq->last_used_idx), "packed");
 
 	END_USE(vq);
 	return ret;
@@ -1848,12 +2033,18 @@
 {
 	struct vring_virtqueue *vq = to_vvq(_vq);
 	bool wrap_counter;
+	bool pending;
 	u16 used_idx;
 
 	wrap_counter = off_wrap >> VRING_PACKED_EVENT_F_WRAP_CTR;
 	used_idx = off_wrap & ~(1 << VRING_PACKED_EVENT_F_WRAP_CTR);
 
-	return is_used_desc_packed(vq, used_idx, wrap_counter);
+	pending = is_used_desc_packed(vq, used_idx, wrap_counter);
+	hp_dma_trace_vq_state(_vq,
+			      pending ? HP_DMA_EVENT_OP_VQ_POLL_HIT :
+					HP_DMA_EVENT_OP_VQ_POLL_MISS,
+			      off_wrap, "packed");
+	return pending;
 }
 
 static bool virtqueue_enable_cb_delayed_packed(struct virtqueue *_vq)
@@ -3236,6 +3460,10 @@
	else
		dma_unmap_page_attrs(vring_dma_dev(vq), map_handle,
				     size, dir, attrs);
+	if (!(attrs & DMA_ATTR_SKIP_CPU_SYNC) &&
+	    hp_dma_trace_complete_on_cpu_sync(dir))
+		HP_DMA_TRACE(vdev, HP_DMA_EVENT_OP_UNMAP, dir, size,
+			     map_handle, "payload", true);
 }
 EXPORT_SYMBOL_GPL(virtqueue_unmap_page_attrs);

@@ -3310,7 +3501,7 @@
 {
 	const struct vring_virtqueue *vq = to_vvq(_vq);
 
-	return vring_mapping_error(vq, addr);
+	return vring_mapping_error(vq, addr, DMA_NONE, 0, "premap");
 }
 EXPORT_SYMBOL_GPL(virtqueue_map_mapping_error);

@@ -3368,6 +3596,9 @@
 	else
 		dma_sync_single_range_for_cpu(vring_dma_dev(vq),
 					      addr, offset, size, dir);
+	if (size && hp_dma_trace_complete_on_cpu_sync(dir))
+		HP_DMA_TRACE(vdev, HP_DMA_EVENT_OP_SYNC_FOR_CPU, dir, size,
+			     addr + offset, "sync", true);
 }
 EXPORT_SYMBOL_GPL(virtqueue_map_sync_single_range_for_cpu);

@@ -3400,6 +3631,9 @@
 	else
 		dma_sync_single_range_for_device(vring_dma_dev(vq), addr,
 						 offset, size, dir);
+	if (size && hp_dma_trace_submit_on_map(dir))
+		HP_DMA_TRACE(vdev, HP_DMA_EVENT_OP_SYNC_FOR_DEVICE, dir, size,
+			     addr + offset, "sync", true);
 }
 EXPORT_SYMBOL_GPL(virtqueue_map_sync_single_range_for_device);
 
diff --git a/include/linux/dma_telemetry_aperture.h b/include/linux/dma_telemetry_aperture.h
index e69de29..69beeff 100644
--- a/include/linux/dma_telemetry_aperture.h
+++ b/include/linux/dma_telemetry_aperture.h
@@ -0,0 +1,108 @@
+#ifndef _LINUX_DMA_TELEMETRY_APERTURE_H
+#define _LINUX_DMA_TELEMETRY_APERTURE_H
+
+#include <linux/dma-direction.h>
+#include <linux/types.h>
+
+/*
+ * Telemetry aperture for HyperArm virtio debug instrumentation.
+ *
+ * This aperture lives inside an otherwise unused virtio transport hole and
+ * exposes a single latched event plus one 64-bit auxiliary payload. For DMA
+ * operations, the payload is normally a DMA address. For virtqueue polling
+ * and completion events, the payload can carry queue-local state such as the
+ * caller's last_used snapshot or the post-consume used index.
+ *
+ * Writers should publish fields in this order:
+ *
+ *   1. HP_DMA_ADDR_LO
+ *   2. HP_DMA_ADDR_HI
+ *   3. HP_DMA_LEN
+ *   4. HP_DMA_EVENT
+ *
+ * HP_DMA_EVENT is written last and acts as the commit point for observers.
+ */
+
+#define HP_DMA_APERTURE_OFFSET              0x0c0u
+#define HP_DMA_APERTURE_SIZE                0x010u
+
+#define HP_DMA_EVENT_OFFSET                 0x0c0u
+#define HP_DMA_ADDR_LO_OFFSET               0x0c4u
+#define HP_DMA_ADDR_HI_OFFSET               0x0c8u
+#define HP_DMA_LEN_OFFSET                   0x0ccu
+
+#define HP_DMA_EVENT(base)                  ((unsigned long)(base) + \
+					    HP_DMA_EVENT_OFFSET)
+#define HP_DMA_ADDR_LO(base)                ((unsigned long)(base) + \
+					    HP_DMA_ADDR_LO_OFFSET)
+#define HP_DMA_ADDR_HI(base)                ((unsigned long)(base) + \
+					    HP_DMA_ADDR_HI_OFFSET)
+#define HP_DMA_LEN(base)                    ((unsigned long)(base) + \
+					    HP_DMA_LEN_OFFSET)
+
+/*
+ * HP_DMA_EVENT register layout
+ *
+ *  31                        16 15         10 9        8 7         0
+ * +----------------------------+-------------+----------+-----------+
+ * |          sequence          |  reserved   |   dir    |  opcode   |
+ * +----------------------------+-------------+----------+-----------+
+ */
+#define HP_DMA_EVENT_OPCODE_SHIFT           0u
+#define HP_DMA_EVENT_OPCODE_MASK            0x000000ffu
+#define HP_DMA_EVENT_DIRECTION_SHIFT        8u
+#define HP_DMA_EVENT_DIRECTION_MASK         0x00000300u
+#define HP_DMA_EVENT_SEQUENCE_SHIFT         16u
+#define HP_DMA_EVENT_SEQUENCE_MASK          0xffff0000u
+
+#define HP_DMA_EVENT_ENCODE(opcode, direction, sequence) \
+	((((u32)(opcode)) << HP_DMA_EVENT_OPCODE_SHIFT) | \
+	 (((u32)(direction)) << HP_DMA_EVENT_DIRECTION_SHIFT) | \
+	 (((u32)(sequence)) << HP_DMA_EVENT_SEQUENCE_SHIFT))
+
+/* HP_DMA_EVENT opcode values */
+#define HP_DMA_EVENT_OP_ALLOC_SUCCESS       0x01u
+#define HP_DMA_EVENT_OP_ALLOC_FAIL          0x02u
+#define HP_DMA_EVENT_OP_FREE                0x03u
+#define HP_DMA_EVENT_OP_MAP                 0x04u
+#define HP_DMA_EVENT_OP_MAP_FAIL            0x05u
+#define HP_DMA_EVENT_OP_UNMAP               0x06u
+#define HP_DMA_EVENT_OP_SYNC_FOR_CPU        0x07u
+#define HP_DMA_EVENT_OP_SYNC_FOR_DEVICE     0x08u
+#define HP_DMA_EVENT_OP_VQ_POLL_HIT         0x09u
+#define HP_DMA_EVENT_OP_VQ_POLL_MISS        0x0au
+#define HP_DMA_EVENT_OP_VQ_GET_BUF          0x0bu
+#define HP_DMA_EVENT_OP_VQ_GET_BUF_EMPTY    0x0cu
+
+/* HP_DMA_EVENT direction values */
+#define HP_DMA_EVENT_DIR_NONE               0x0u
+#define HP_DMA_EVENT_DIR_TO_DEVICE          0x1u
+#define HP_DMA_EVENT_DIR_FROM_DEVICE        0x2u
+#define HP_DMA_EVENT_DIR_BIDIRECTIONAL      0x3u
+
+static inline u32 hp_dma_addr_lo(u64 addr)
+{
+	return (u32)(addr & 0xffffffffull);
+}
+
+static inline u32 hp_dma_addr_hi(u64 addr)
+{
+	return (u32)((addr >> 32) & 0xffffffffull);
+}
+
+static inline u32 hp_dma_event_dir(enum dma_data_direction dir)
+{
+	switch (dir) {
+	case DMA_TO_DEVICE:
+		return HP_DMA_EVENT_DIR_TO_DEVICE;
+	case DMA_FROM_DEVICE:
+		return HP_DMA_EVENT_DIR_FROM_DEVICE;
+	case DMA_BIDIRECTIONAL:
+		return HP_DMA_EVENT_DIR_BIDIRECTIONAL;
+	case DMA_NONE:
+	default:
+		return HP_DMA_EVENT_DIR_NONE;
+	}
+}
+
+#endif /* _LINUX_DMA_TELEMETRY_APERTURE_H */
diff --git a/drivers/virtio/virtio_mmio.c b/drivers/virtio/virtio_mmio.c
index 56128b9..da2ff00 100644
--- a/drivers/virtio/virtio_mmio.c
+++ b/drivers/virtio/virtio_mmio.c
@@ -88,6 +88,29 @@ struct virtio_mmio_device {
 	void __iomem *base;
 	unsigned long version;
 };
+static bool hyperarm_virtio_oracle_bug_enabled = true;
+module_param_named(hyperarm_oracle_bug, hyperarm_virtio_oracle_bug_enabled, bool, 0644);
+MODULE_PARM_DESC(hyperarm_oracle_bug,
+		 "Enable HyperArm fuzz-oracle driver panic");
+
+#define HYPERARM_VIRTIO_ORACLE_MAGIC0 0xa5u
+#define HYPERARM_VIRTIO_ORACLE_MAGIC1 0x5au
+
+static noinline void hyperarm_virtio_oracle_bug(void)
+{
+	BUG();
+}
+
+static void hyperarm_virtio_oracle_check(u32 value)
+{
+	if (!hyperarm_virtio_oracle_bug_enabled ||
+	    ((value & 0xffu) != HYPERARM_VIRTIO_ORACLE_MAGIC0 &&
+	     (value & 0xffu) != HYPERARM_VIRTIO_ORACLE_MAGIC1))
+		return;
+
+	pr_err("HyperArm oracle test bug triggered value=0x%08x\n", value);
+	hyperarm_virtio_oracle_bug();
+}
 
 /* Configuration interface */
 
@@ -95,13 +118,18 @@ static u64 vm_get_features(struct virtio_device *vdev)
 {
 	struct virtio_mmio_device *vm_dev = to_virtio_mmio_device(vdev);
 	u64 features;
+	u32 feature_hi;
+	u32 feature_lo;
 
 	writel(1, vm_dev->base + VIRTIO_MMIO_DEVICE_FEATURES_SEL);
-	features = readl(vm_dev->base + VIRTIO_MMIO_DEVICE_FEATURES);
-	features <<= 32;
+	feature_hi = readl(vm_dev->base + VIRTIO_MMIO_DEVICE_FEATURES);
+	hyperarm_virtio_oracle_check(feature_hi);
+	features = (u64)feature_hi << 32;
 
 	writel(0, vm_dev->base + VIRTIO_MMIO_DEVICE_FEATURES_SEL);
-	features |= readl(vm_dev->base + VIRTIO_MMIO_DEVICE_FEATURES);
+	feature_lo = readl(vm_dev->base + VIRTIO_MMIO_DEVICE_FEATURES);
+	hyperarm_virtio_oracle_check(feature_lo);
+	features |= feature_lo;
 
 	return features;
 }
@@ -372,6 +400,7 @@ static struct virtqueue *vm_setup_vq(struct virtio_device *vdev, unsigned int index,
 	}
 
 	num = readl(vm_dev->base + VIRTIO_MMIO_QUEUE_NUM_MAX);
+	hyperarm_virtio_oracle_check(num);
 	if (num == 0) {
 		err = -ENOENT;
 		goto error_new_virtqueue;
