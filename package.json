{
  "name": "morpheus",
  "private": true,
  "version": "0.4.3",
  "description": "Morpheus is a research platform from North Star Systems Security Lab (NS3L).",
  "packageManager": "pnpm@10.8.1",
  "scripts": {
    "setup": "pnpm install && pnpm run build && pnpm run install:bin",
    "build": "pnpm run build:apps",
    "build:apps": "pnpm --filter @morpheus/app build",
    "cache:enable": "node scripts/enable-cache.mjs",
    "ci:smoke:tool:buildroot": "node apps/morpheus/dist/cli.js --json --config tests/morpheus.yaml workflow run --name buildroot-build-ci",
    "ci:smoke:tool:microkit-sdk": "node apps/morpheus/dist/cli.js --json --config tests/morpheus.yaml workflow run --name microkit-sdk-build-ci",
    "ci:smoke:tool:qemu": "node apps/morpheus/dist/cli.js --json --config tests/morpheus.yaml workflow run --name qemu-build-ci",
    "ci:smoke:tool:sel4": "node apps/morpheus/dist/cli.js --json --config tests/morpheus.yaml workflow run --name sel4-fetch-patch-ci",
    "ci:smoke:tool:libvmm": "node apps/morpheus/dist/cli.js --json --config tests/morpheus.yaml workflow run --name libvmm-build-ci",
    "ci:pre-push": "pnpm --filter @morpheus/app test:repo-structure && pnpm test:ci-config && pnpm lint && pnpm test",
    "test:ci-config": "pnpm --filter @morpheus/app test:ci-config",
    "test": "pnpm --filter @morpheus/app test",
    "lint": "pnpm --filter @morpheus/app lint",
    "install:bin": "node scripts/install-bin.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
