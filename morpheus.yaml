workspace:
  root: ${MORPHEUS_DATA_ROOT}/workspaces/root
cache:
  root: ./.cache
  namespace: root
  downloads: global
  builds: global
  src: global
tools:
  buildroot:
    seed-dir: ./tools/buildroot/tests/fixtures/minimal-buildroot
    patch-dir: ./tools/buildroot/patches/demo
  libvmm:
    seed-dir: ./tools/libvmm/tests/fixtures/minimal-libvmm-src
    patch-dir: ./tools/libvmm/patches/demo
    example: virtio
  microkit-sdk:
    seed-dir: ./tools/microkit-sdk/tests/fixtures/minimal-microkit-sdk-src
    patch-dir: ./tools/microkit-sdk/patches/demo
    toolchain-version: fixture
    toolchain-prefix-aarch64: aarch64-none-elf
  llbase:
    mode: local
    source: ./tools/llbase
    build-dir-key: latest
    family: latest
    pull-image: "false"
    build-image: false
    artifacts:
      - source-dir
      - output-dir
      - runtime-helper
      - runtime-contract
  devilang:
    mode: local
    build-dir-key: fixture
    clang: 15
    artifacts:
      - build-dir
      - output-dir
      - manifest
      - log-file
  nqc2:
    build-version: dev
    build-dir-key: default
  qemu:
    seed-dir: ./tools/qemu/tests/fixtures/minimal-qemu-src
    patch-dir: ./tools/qemu/patches/demo
  libafl:
    seed-dir: ./tools/libafl/tests/fixtures/minimal-libafl-src
    patch-dir: ${MORPHEUS_DATA_ROOT}/workspaces/hyperarm/tools/libafl/patches/overlay
  pkvm-aarch64:
    git-url: https://github.com/vrosendahl/pkvm-aarch64.git
    patch-dir: ./tools/pkvm-aarch64/patches/overlay
  nvirsh:
    source: ./tools/nvirsh/profiles/qemu-debian-arm
    profile: qemu-debian-arm64
    build-dir-key: qemu-debian-arm64
    reuse-build-dir: true
    firmware: /usr/share/qemu-efi-aarch64/QEMU_EFI.fd
    dependencies:
      qemu:
        tool: qemu
        artifact: qemu-system-aarch64
      buildroot:
        tool: buildroot
        artifact: output-dir
  sel4:
    seed-dir: ./tools/sel4/tests/fixtures/minimal-sel4-src
    patch-dir: ./tools/sel4/patches/demo
workflows:
  llbase-build-ci:
    category: build
    steps:
      - id: llbase_build
        tool: llbase
        command: build
  llbic-build-ci:
    category: build
    steps:
      - id: llbic_inspect
        tool: llbic
        command: inspect
        args:
          - --target
          - ./tools/llbic/tests/fixtures/linux-6.18.16-arm64-clang15/llbic.json
  llcg-build-ci:
    category: build
    steps:
      - id: llcg_inspect
        tool: llcg
        command: inspect
        args:
          - --target
          - ./tools/llcg/tests/fixtures/minimal-llcg-output/llcg-manifest.json
  devilang-build-ci:
    category: build
    steps:
      - id: llbase_build
        tool: llbase
        command: build
      - id: llcg_build
        tool: llcg
        command: build
        args:
          - --llbase-contract
          - "{{steps.llbase_build.artifacts.runtime-contract.location}}"
      - id: llbic_inspect
        tool: llbic
        command: inspect
        args:
          - --target
          - ./tools/llbic/tests/fixtures/linux-6.18.16-arm64-clang15/llbic.json
      - id: devilang_build
        tool: devilang
        command: build
        args:
          - --llbase-contract
          - "{{steps.llbase_build.artifacts.runtime-contract.location}}"
          - --llcg-build-dir
          - "{{steps.llcg_build.artifacts.build-dir.location}}"
      - id: devilang_audit
        tool: devilang
        command: audit-state
        args:
          - --input
          - ./tools/devilang/tests/fixtures/audit-state/pass.state
      - id: devilang_audit_test
        tool: devilang
        command: test-audit-state
      - id: devilang_static_checks
        tool: devilang
        command: test-static-checks
      - id: devilang_phase_models
        tool: devilang
        command: test-phase-models
        args:
          - --build-dir
          - "{{steps.devilang_build.artifacts.build-dir.location}}"
          - --llcg-build-dir
          - "{{steps.llcg_build.artifacts.build-dir.location}}"
  libafl-build-ci:
    category: build
    steps:
      - id: libafl_fetch
        tool: libafl
        command: fetch
        args:
          - --build-version
          - ffbb151f34d4d486ffc8e24fc105571c4133a149
      - id: libafl_patch
        tool: libafl
        command: patch
        args:
          - --source
          - "{{steps.libafl_fetch.artifacts.source-dir.location}}"
      - id: libafl_build
        tool: libafl
        command: build
        args:
          - --source
          - "{{steps.libafl_patch.artifacts.source-dir.location}}"
  libvmm-build-ci:
    category: build
    steps:
      - id: sel4_fetch
        tool: sel4
        command: fetch
        args:
          - --build-version
          - fixture
      - id: sel4_patch
        tool: sel4
        command: patch
        args:
          - --path
          - "{{steps.sel4_fetch.artifacts.source-dir.location}}"
      - id: microkit_sdk_fetch
        tool: microkit-sdk
        command: fetch
        args:
          - --build-version
          - fixture
      - id: microkit_sdk_patch
        tool: microkit-sdk
        command: patch
        args:
          - --path
          - "{{steps.microkit_sdk_fetch.artifacts.source-dir.location}}"
      - id: microkit_sdk_build
        tool: microkit-sdk
        command: build
        args:
          - --path
          - "{{steps.microkit_sdk_patch.artifacts.source-dir.location}}"
          - --sel4
          - "{{steps.sel4_patch.artifacts.source-dir.location}}"
          - --boards
          - qemu_virt_aarch64
          - --configs
          - debug
      - id: libvmm_fetch
        tool: libvmm
        command: fetch
      - id: libvmm_patch
        tool: libvmm
        command: patch
        args:
          - --source
          - "{{steps.libvmm_fetch.artifacts.libvmm-dir.location}}"
      - id: libvmm_build
        tool: libvmm
        command: build
        args:
          - --source
          - "{{steps.libvmm_patch.artifacts.libvmm-dir.location}}"
          - --microkit-sdk
          - "{{steps.microkit_sdk_build.artifacts.sdk-dir.location}}"
          - --toolchain-bin-dir
          - "{{steps.microkit_sdk_build.artifacts.toolchain-dir.location}}/bin"
  microkit-sdk-build-ci:
    category: build
    steps:
      - id: sel4_fetch
        tool: sel4
        command: fetch
        args:
          - --build-version
          - fixture
      - id: sel4_patch
        tool: sel4
        command: patch
        args:
          - --path
          - "{{steps.sel4_fetch.artifacts.source-dir.location}}"
      - id: microkit_sdk_fetch
        tool: microkit-sdk
        command: fetch
        args:
          - --build-version
          - fixture
      - id: microkit_sdk_patch
        tool: microkit-sdk
        command: patch
        args:
          - --path
          - "{{steps.microkit_sdk_fetch.artifacts.source-dir.location}}"
      - id: microkit_sdk_build
        tool: microkit-sdk
        command: build
        args:
          - --path
          - "{{steps.microkit_sdk_patch.artifacts.source-dir.location}}"
          - --sel4
          - "{{steps.sel4_patch.artifacts.source-dir.location}}"
          - --boards
          - qemu_virt_aarch64
          - --configs
          - debug,release
  sel4-fetch-patch-ci:
    category: build
    steps:
      - id: sel4_fetch
        tool: sel4
        command: fetch
        args:
          - --build-version
          - fixture
      - id: sel4_patch
        tool: sel4
        command: patch
        args:
          - --path
          - "{{steps.sel4_fetch.artifacts.source-dir.location}}"
  buildroot-build-ci:
    category: build
    steps:
      - id: buildroot_fetch
        tool: buildroot
        command: fetch
        args:
          - --build-version
          - fixture
      - id: buildroot_patch
        tool: buildroot
        command: patch
        args:
          - --source
          - "{{steps.buildroot_fetch.artifacts.source-dir.location}}"
      - id: buildroot_build
        tool: buildroot
        command: build
        args:
          - --source
          - "{{steps.buildroot_patch.artifacts.source-dir.location}}"
          - --build-dir-key
          - default
          - --defconfig
          - qemu_aarch64_virt_defconfig
  buildroot-fetch-ci:
    category: build
    steps:
      - id: buildroot_fetch
        tool: buildroot
        command: fetch
        args:
          - --build-version
          - fixture
  buildroot-patch-ci:
    category: build
    steps:
      - id: buildroot_fetch
        tool: buildroot
        command: fetch
        args:
          - --build-version
          - fixture
      - id: buildroot_patch
        tool: buildroot
        command: patch
        args:
          - --source
          - "{{steps.buildroot_fetch.artifacts.source-dir.location}}"
  qemu-fetch-ci:
    category: build
    steps:
      - id: qemu_fetch
        tool: qemu
        command: fetch
        args:
          - --build-version
          - fixture
  qemu-patch-ci:
    category: build
    steps:
      - id: qemu_fetch
        tool: qemu
        command: fetch
        args:
          - --build-version
          - fixture
      - id: qemu_patch
        tool: qemu
        command: patch
        args:
          - --source
          - "{{steps.qemu_fetch.artifacts.source-dir.location}}"
  qemu-build-ci:
    category: build
    steps:
      - id: qemu_fetch
        tool: qemu
        command: fetch
        args:
          - --build-version
          - fixture
      - id: qemu_patch
        tool: qemu
        command: patch
        args:
          - --source
          - "{{steps.qemu_fetch.artifacts.source-dir.location}}"
      - id: qemu_build
        tool: qemu
        command: build
        args:
          - --source
          - "{{steps.qemu_patch.artifacts.source-dir.location}}"
          - --build-dir-key
          - default
  nqc2-build-ci:
    category: build
    steps:
      - id: qemu_fetch
        tool: qemu
        command: fetch
        args:
          - --build-version
          - fixture
      - id: qemu_patch
        tool: qemu
        command: patch
        args:
          - --source
          - "{{steps.qemu_fetch.artifacts.source-dir.location}}"
      - id: qemu_build
        tool: qemu
        command: build
        args:
          - --source
          - "{{steps.qemu_patch.artifacts.source-dir.location}}"
          - --build-dir-key
          - default
      - id: nqc2_fetch
        tool: nqc2
        command: fetch
      - id: nqc2_build
        tool: nqc2
        command: build
        args:
          - --source
          - "{{steps.nqc2_fetch.artifacts.source-dir.location}}"
          - --qemu
          - "{{steps.qemu_build.artifacts.qemu-system-aarch64.location}}"
  nvirsh-qemu-arm64-vm-exec-ci:
    category: run
    stages:
      - id: buildroot_fetch
        tool: buildroot
        command: fetch
        stageId: buildroot
        stageName: buildroot
        stageIndex: 0
        stageStepIndex: 0
      - id: buildroot_patch
        tool: buildroot
        command: patch
        args:
          - --source
          - "{{steps.buildroot_fetch.artifacts.source-dir.location}}"
        stageId: buildroot
        stageName: buildroot
        stageIndex: 0
        stageStepIndex: 1
      - id: buildroot_build
        tool: buildroot
        command: build
        args:
          - --source
          - "{{steps.buildroot_patch.artifacts.source-dir.location}}"
        stageId: buildroot
        stageName: buildroot
        stageIndex: 0
        stageStepIndex: 2
      - id: qemu_fetch
        tool: qemu
        command: fetch
        stageId: qemu
        stageName: qemu
        stageIndex: 1
        stageStepIndex: 0
      - id: qemu_patch
        tool: qemu
        command: patch
        args:
          - --source
          - "{{steps.qemu_fetch.artifacts.source-dir.location}}"
        stageId: qemu
        stageName: qemu
        stageIndex: 1
        stageStepIndex: 1
      - id: qemu_build
        tool: qemu
        command: build
        args:
          - --source
          - "{{steps.qemu_patch.artifacts.source-dir.location}}"
        stageId: qemu
        stageName: qemu
        stageIndex: 1
        stageStepIndex: 2
      - id: nvirsh_fetch
        tool: nvirsh
        command: fetch
        stageId: nvirsh
        stageName: nvirsh
        stageIndex: 2
        stageStepIndex: 0
      - id: nvirsh_build
        tool: nvirsh
        command: build
        args:
          - --qemu
          - "{{steps.qemu_build.artifacts.qemu-system-aarch64.location}}"
          - --buildroot-output-dir
          - "{{steps.buildroot_build.artifacts.output-dir.location}}"
          - --guest-qemu-source
          - "{{steps.qemu_patch.artifacts.source-dir.location}}"
          - --l2-mode
          - vm
        stageId: nvirsh
        stageName: nvirsh
        stageIndex: 2
        stageStepIndex: 1
      - id: nvirsh_exec
        tool: nvirsh
        command: exec
        args:
          - --phase
          - launch
          - --detach
        timeout-seconds: 60
        stageId: nvirsh-exec
        stageName: nvirsh-exec
        stageIndex: 3
        stageStepIndex: 0
      - id: nvirsh_stop
        tool: nvirsh
        command: stop
        args:
          - --run-dir
          - "{{steps.nvirsh_exec.details.run_dir}}"
        stageId: nvirsh-exec
        stageName: nvirsh-exec
        stageIndex: 3
        stageStepIndex: 1
  nvirsh-qemu-arm64-cvm-exec-ci:
    category: run
    stages:
      - id: buildroot_fetch
        tool: buildroot
        command: fetch
        stageId: buildroot
        stageName: buildroot
        stageIndex: 0
        stageStepIndex: 0
      - id: buildroot_patch
        tool: buildroot
        command: patch
        args:
          - --source
          - "{{steps.buildroot_fetch.artifacts.source-dir.location}}"
        stageId: buildroot
        stageName: buildroot
        stageIndex: 0
        stageStepIndex: 1
      - id: buildroot_build
        tool: buildroot
        command: build
        args:
          - --source
          - "{{steps.buildroot_patch.artifacts.source-dir.location}}"
        stageId: buildroot
        stageName: buildroot
        stageIndex: 0
        stageStepIndex: 2
      - id: qemu_fetch
        tool: qemu
        command: fetch
        stageId: qemu
        stageName: qemu
        stageIndex: 1
        stageStepIndex: 0
      - id: qemu_patch
        tool: qemu
        command: patch
        args:
          - --source
          - "{{steps.qemu_fetch.artifacts.source-dir.location}}"
        stageId: qemu
        stageName: qemu
        stageIndex: 1
        stageStepIndex: 1
      - id: qemu_build
        tool: qemu
        command: build
        args:
          - --source
          - "{{steps.qemu_patch.artifacts.source-dir.location}}"
        stageId: qemu
        stageName: qemu
        stageIndex: 1
        stageStepIndex: 2
      - id: nvirsh_fetch
        tool: nvirsh
        command: fetch
        stageId: nvirsh
        stageName: nvirsh
        stageIndex: 2
        stageStepIndex: 0
      - id: nvirsh_build
        tool: nvirsh
        command: build
        args:
          - --qemu
          - "{{steps.qemu_build.artifacts.qemu-system-aarch64.location}}"
          - --buildroot-output-dir
          - "{{steps.buildroot_build.artifacts.output-dir.location}}"
          - --guest-qemu-source
          - "{{steps.qemu_patch.artifacts.source-dir.location}}"
          - --l2-mode
          - cvm
        stageId: nvirsh
        stageName: nvirsh
        stageIndex: 2
        stageStepIndex: 1
      - id: nvirsh_exec
        tool: nvirsh
        command: exec
        args:
          - --phase
          - launch
          - --detach
        timeout-seconds: 10
        stageId: nvirsh-exec
        stageName: nvirsh-exec
        stageIndex: 3
        stageStepIndex: 0
      - id: nvirsh_stop
        tool: nvirsh
        command: stop
        args:
          - --run-dir
          - "{{steps.nvirsh_exec.details.run_dir}}"
        stageId: nvirsh-exec
        stageName: nvirsh-exec
        stageIndex: 3
        stageStepIndex: 1
  # pkvm-aarch64-build-ci:
  #   Disabled for now.
  #   Current pKVM image targets still depend on unstable rootless mount/chroot
  #   behavior in CI, and the upstream tree still needs a cleaner CI-safe smoke
  #   contract before this workflow should be re-enabled at repo root.
