# `libafl`

Managed LibAFL source-tree tooling for the `libafl_nesting` crate.

Commands:

- `fetch`
- `patch`
- `build`
- `inspect`

This tool fetches the LibAFL repository, patches in the
`libafl_nesting` crate, and builds the guest stub artifact.
