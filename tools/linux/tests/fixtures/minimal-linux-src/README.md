This is a synthetic Linux source tree fixture.

It exists only for CLI smoke tests. It validates that `linux build`
stages an `arch/arm64/boot/Image` and a `vmlinux` artifact without
building a real kernel.
